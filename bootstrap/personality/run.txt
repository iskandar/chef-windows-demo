$Dir = "C:\cloud-automation"
Start-Transcript -Path $Dir\run.log -Append
$Hostname = $env:COMPUTERNAME
$PublicIP = ((Get-NetIPConfiguration).IPv4Address | Where-Object {$_.InterfaceAlias -eq "public0"}).IpAddress
@{
    "CallbackURLs" = @("{{ node_callback_url }}")
} | ConvertTo-Json | Out-File $Dir\config.json
$BootURL = Get-Content "$Dir\bootstrap.url" -Raw
$BootFile = "$Dir\bootstrap.ps1"
(New-Object System.Net.WebClient).DownloadFile($BootURL, $BootFile)
Invoke-Expression -Command $BootFile
Stop-Transcript